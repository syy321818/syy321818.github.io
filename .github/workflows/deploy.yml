name: Force Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'source/**'
      - '_config.yml'
      - '_config.next.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Hexo
        run: |
          npm install hexo-cli -g
          npm install

      - name: Build
        run: hexo g

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages  # 强制创建这个分支
