name: Fixed Auto Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'source/**'
      - '*.yml'
      - 'package.json'
  workflow_dispatch:

# 👇 这一段必须写在最外面，确保拥有最高权限
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 拉取代码
        uses: actions/checkout@v3

      - name: 2. 准备环境
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 3. 安装依赖和主题 (关键修复步骤)
        run: |
          npm install
          npm install hexo-cli -g
          npm install hexo-deployer-git --save
          
          # 👇 自动下载主题！防止因为缺少 themes 文件夹而报错
          mkdir -p themes
          git clone https://github.com/next-theme/hexo-theme-next themes/next || echo "Theme already exists"

      - name: 4. 生成网页
        run: |
          npx hexo clean
          npx hexo g

      - name: 5. 强制发布
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          force_orphan: true
